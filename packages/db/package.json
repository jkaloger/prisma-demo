{
  "name": "db",
  "version": "0.0.0",
  "main": "./index.ts",
  "types": "./index.ts",
  "scripts": {
    "dev": "prisma studio",
    "db:generate": "prisma generate",
    "db:push": "prisma db push --skip-generate",
    "db:connect": "pscale connect prisma-test"
  },
  "dependencies": {
    "@prisma/client": "^5.2.0"
  },
  "devDependencies": {
    "prisma": "^5.2.0"
  }
}
